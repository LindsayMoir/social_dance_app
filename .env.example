# Email Notification Configuration
# Configure these variables to enable email notifications for validation reports

# Sender email address
NOTIFICATION_EMAIL_FROM=your-app@gmail.com

# Recipient email address (comma-separated for multiple recipients)
NOTIFICATION_EMAIL_TO=admin@example.com,developer@example.com

# SMTP Server Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# SMTP Authentication
SMTP_USERNAME=your-app@gmail.com
SMTP_PASSWORD=your_app_specific_password

# For Gmail Users:
# 1. Enable 2-factor authentication on your Google account
# 2. Generate an "App Password" at: https://myaccount.google.com/apppasswords
# 3. Use the app password as SMTP_PASSWORD (not your regular Gmail password)
#
# For Other Email Providers:
# - Update SMTP_HOST and SMTP_PORT accordingly
# - Use your email provider's SMTP credentials

# Database Configuration (existing)
DATABASE_CONNECTION_STRING=postgresql://<USERNAME>:<PASSWORD>@localhost:5432/social_dance_db

# API Keys (existing)
OPENAI_API_KEY=your_openai_api_key
ANTHROPIC_API_KEY=your_anthropic_api_key
MISTRAL_API_KEY=your_mistral_api_key
